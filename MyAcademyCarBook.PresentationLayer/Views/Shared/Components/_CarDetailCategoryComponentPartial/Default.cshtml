@model List<MyAcademyCarBook.DtoLayer.CarCategoryDto.CarCategoryDto>

<div class="sidebar-box">
    <div class="categories">
        <h3>Kategoriler</h3>

        @foreach (var item in Model)
        {
            <li> <a href="#">@item.CategoryName<span>@item.Count</span></a> </li>
        }
    </div>
